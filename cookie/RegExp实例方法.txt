// 创建 RegExp对象：
var reg = new RegExp(pattern)

// 实例方法：

reg.test(str)  // 返回Boolean
reg.exec(str)  // 返回对象












var str = "this is string"

reg.exec(str)
str.match(reg)

